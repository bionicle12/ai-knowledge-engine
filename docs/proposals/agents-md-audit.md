# C1 — разбор `AGENTS.md.template` по строкам

Источник: `knowledge-base/templates/AGENTS.md.template`  
Дата: 2026-08-23  
Итерация: C1 плана `2026-08-23-boosty-kb-improvements.md`  
Правило этого файла: разбор, затем C2 по **вердиктам владельца** (2026-08-23).

Категории:

| # | Имя | Что делать в C2, если владелец согласен |
|---|-----|----------------------------------------|
| 1 | инвариант | оставить |
| 2 | дубль обученного | удалить |
| 3 | реликт | удалить |
| 4 | правило-90% | переписать условием, не абсолютом |
| 5 | конфликт / дубль внутри базы | оставить одну сторону |

INDEX (`I1`–`I4`) в опрос не входил — **не трогаем** (план: I1 дословно).  
Таблица `### Commands` и `AI-KE:INVARIANT` — не в разборе.

---

## Уже закрыто, в разбор не входит

| Что | Почему |
|-----|--------|
| `<!-- AI-KE:INVARIANT -->` `forbidden` + `language` | Категория 1 автоматически. Не режем. |
| Таблица `### Commands` | Решение владельца: не трогать. `!heal` / `!refactor` добавляются строками, таблицу не ужимаем. |
| Стоппер «откажи `!` до opening line» | Снят в A1. |

Замечание для C2: блок `AI-KE:INDEX` **управляемый**. Текст внутри него в C2 не менялся.

---

## Таблица

| ID | Где | Текст (сжато) | Предложение | Зачем | Вердикт |
|----|-----|---------------|-------------|-------|---------|
| H1 | шапка | «You are a primary-instruction file… takes precedence» | 1 | Локальный ритуал + приоритет над generic chat. | **оставить** |
| H2 | шапка | `Generated from … instructions_version` | 1 | Бухгалтерия деплоя. | **оставить** |
| P1 | Purpose | роль `{{PRIMARY_ROLE}}` + зачем база | 1 | Роль и «не код». | **оставить** |
| C0 | CRITICAL | opening line = загрузить базу, skim, короткое ack | 1 | Страховка. Сжато в A1. | **оставить** |
| X1 | Context | 5 путей | 1 | Карта этой базы. | **оставить** |
| R1 | Reading priority | AGENTS → config → profile → principles → voice → rest | 1 | Порядок *старта сессии*. | **оставить** (оба порядка) |
| I1 | INDEX | «Never read a full-base index dump» + core + ≤1 domain pack | **1** | Настоящий инвариант загрузки. | **оставить** (не в опросе) |
| I2 | INDEX | Route first: routing-table → PACKS_STATUS | 1 | Локальный маршрут. | **оставить** |
| I3 | INDEX | Targeted edits — файлы, не паки | 1 | Иначе правки по XML. | **оставить** |
| I4 | INDEX | Lost thread → два маленьких файла, один пак | 1 | Деградация длинных сессий. | **оставить** |
| U1 | How to use | разбирать `review/needs-ai-decision/` | 1 | Очередь базы. | **оставить** |
| U2 | How to use | выводы review → `knowledge/` | 1 | Куда класть смысл. | **оставить** |
| U3 | How to use | бинарь описать в `assets-index/` | 1 | Схема базы. | **оставить** |
| A1 | Chat-attached | не ingest без подтверждения (шаги 1, 4) | 1 | Безопасность. | **оставить** |
| A2 | Chat-attached | шаги 2–3, 5–8 | **4** | План: два критерия. | **оставить все 8 шагов** |
| F1 | Feedback | «auto-detects when meaningful material has accumulated» | **4** | Размытый заказчик работы. | **убрать auto-detect; писать по `!save` / явной просьбе** |
| F2 | Feedback | писать в `interactions/sessions/` | 1 | Куда писать. | **оставить** |
| F3 | Feedback | `!save` — сразу | 5 | Есть в таблице команд. | **оставить в таблице; в секции не повторять чеклист** |
| V1 | после Commands | `!view` → `kb_view.py` | **5** | Нужно «не строй граф ИИ». | **оставить одну фразу: скрипты, не ИИ** |
| V2 | после Commands | список фич вьюера | **2** | Модель не зубрится UI. | **удалить** |
| S1 | после Commands | export/import/merge = скрипт + смысл | **5** | Дубль таблицы + Forbidden. | **сжать в ту же одну фразу, что V1** |
| S2 | после Commands | CLI-строки export/import/merge | **5** | Дубль колонки «What it does». | **удалить** |
| L1 | Knowledge lifecycle | 7 буллетов + `last_verified` | **5** | Есть в `KNOWLEDGE_STRUCTURE.md`. | **оставить список в AGENTS.md** |
| M1 | Operating mode | «прочитай `mode`, примени `mode_profiles`» | 1 | Куда смотреть. | **оставить** |
| M2 | Operating mode | таблица + длинные списки default/super | **2** | Цифры живут в конфиге. | **короткая таблица default vs super; списки удалить** |
| M3 | Operating mode | предупреждение «super сжигает бюджет» | 1 | Стоп-кран. | **оставить** |
| B1 | Context budget | insights → opinions → domain → … | **5** | Второй порядок (приоритет фактов). | **оставить** (оба порядка) |
| B2 | Context budget | always start with routing table | **5** | То же, что I2. | **оставить** (рядом с B1) |
| B3 | Context budget | «at most 7 files» / «if > 5, stop» | **4** | Цифры с потолка. | **оставить 7; удалить «> 5, stop»** |
| B4 | Context budget | summarize before loading more | 2 | Сильная модель и так делает. | **оставить** (не в опросе; коротко) |
| B5 | Context budget | prefer high `importance` / `last_accessed` | 1 | Поля схемы. | **оставить** |
| B6 | Context budget | фильтр `valid_from` / `valid_until` | 1 | Поля схемы. | **оставить** |
| B7 | Context budget | при чтении крутить `last_accessed` | **4** | Скрытая запись на каждый read. | **только если страница повлияла на ответ** |
| T1 | Token budget | вся секция | **2** | Дубль M2. | **удалить целиком** |

---

## Сводка вердиктов (владелец, 2026-08-23)

| Кластер | Выбор |
|---------|--------|
| Chat-attached | Все 8 шагов |
| Operating mode + Token budget | Короткая таблица + предупреждение super; Token budget удалить |
| Context caps | Лимит 7 оставить; «> 5, stop» убрать |
| Lifecycle + простыня команд | Lifecycle оставить; у команд одна фраза «скрипты, не ИИ» |
| Два порядка + Feedback | Оба порядка; Feedback только `!save` / явная просьба |
| Access tracking | `last_accessed` только если страница повлияла на ответ |
| INDEX | Не трогали |

Цель плана (~7,3 KiB) **не обязательна**: владелец оставил больше текста, чем сжатие в 4.3.
После C2+C3 и добавления команд итераций D/E шаблон = **8990 B**. Меряют lint и doctor не
шаблон, а развёрнутый файл: `kb_upgrade` дописывает управляемый блок `!view` (454 B), итого
**9419 B**. Поэтому порог `instructions_lint.agents_max_bytes` = **10240 B (10 KiB)** —
с запасом ~820 B на локальные правки владельца. Тест
`test_deployed_agents_md_stays_under_the_lint_threshold` стережёт именно развёрнутый размер.

---

## C2 сделано

Шаблон переписан по таблице выше. Eval lite на *этом* репозитории движка не гонялся: здесь нет развёрнутой базы с заполненным `eval/QUESTIONS.md`.
