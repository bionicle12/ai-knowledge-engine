# Knowledge Structure — {{KB_NAME}}

> Map of folders in this knowledge base. What goes where, what gets indexed, and the rules for each layer.
> Generated from `templates/KNOWLEDGE_STRUCTURE.md.template` (instructions_version {{KB_INSTRUCTIONS_VERSION}}).

## Top-level layers

```
{{KB_NAME}}/
├── AGENTS.md                   # Agent instructions (read this first)
├── KNOWLEDGE_STRUCTURE.md      # ← you are here
├── DATA_PLACEMENT_EXAMPLES.md  # "Got this file → put it here" (per role)
├── kb.config.yml               # Config: role, entities, mode, rules
├── repomix.config.json         # Indexer config
├── requirements.txt            # Python dependencies
├── reindex.sh                  # Run pipeline + reindex
├── watcher.sh                  # File watcher (auto pipeline)
├── lint.sh                     # Health check
├── doctor.sh                   # Smoke-test
│
├── scripts/                    # Reference implementations (do not modify lightly)
├── shell/                      # Wrapper scripts
│
├── raw/                        # 🚫 Raw materials (NOT indexed)
│   ├── documents/unsorted/
│   ├── reference/unsorted/
│   ├── work/unsorted/
│   ├── chats/unsorted/
│   ├── media/unsorted/
│   ├── personal-context/unsorted/
│   └── unsorted/               # When you don't know where it goes
│
├── processed/                  # 🚫 Converted artifacts (NOT indexed)
│   ├── markdown/
│   ├── transcripts/
│   ├── ocr/
│   ├── tables/
│   ├── extracted-metadata/
│   └── nlp-meta/
│
├── knowledge/                  # ✅ Clean knowledge — INDEXED
│   ├── profile/                # Who the author is
│   ├── principles/             # Working principles, quality bars
│   ├── voice/                  # Writing/speaking style
│   ├── domain/                 # Domain knowledge, market, technology
│   ├── projects/               # Projects, cases, results
│   ├── decisions/              # Immutable decision records
│   ├── playbooks/              # Repeatable workflows
│   ├── insights/               # Synthesized higher-level conclusions
│   ├── opinions/               # Subjective takes with confidence
│   ├── timelines/              # Chronologies, growth paths
│   ├── routing/                # Navigation tables for large bases
│   ├── open-questions/         # Unresolved questions
│   └── _archive/               # Superseded entries
│
├── assets/                     # 🚫 Binary originals (NOT indexed)
│   ├── documents/
│   ├── presentations/
│   ├── media/
│   ├── images/
│   └── archives/
│
├── assets-index/               # ✅ Markdown descriptions of assets — INDEXED
│   ├── documents.md
│   ├── presentations.md
│   ├── media.md
│   ├── images.md
│   └── archives.md
│
├── review/                     # 🚫 Review queue (NOT indexed)
│   ├── needs-classification/
│   ├── needs-ai-decision/
│   ├── needs-redaction/
│   ├── needs-merge/            # Cross-base conflicts → resolved by !merge
│   ├── needs-heal/             # Catch-up after upgrade → !heal
│   └── excluded-sensitive/
│
├── interactions/               # 🚫 Session feedback loop (NOT indexed directly)
│   ├── sessions/
│   ├── insights/
│   ├── meta-reviews/
│   ├── init/                   # STRUCTURE_VARIANTS.md from kb_structure.py
│   └── quiz/                   # !quiz answer sheets
│
├── eval/                       # 🚫 Manual instruction probes (NOT indexed)
│   ├── QUESTIONS.md            # Three typical questions the owner asks
│   └── results/                # Before/after answer dumps — keep in git
│
├── sync/                       # 🚫 Cross-base import/export (NOT indexed)
│   ├── inbox/                  # Drop bundles from the other machine here
│   ├── outbox/                 # Bundles produced by export
│   ├── applied/                # Bundles already imported
│   ├── backups/                # Pre-import snapshots
│   └── reports/                # Merge reports
│
└── .repomix/                   # 🚫 Generated index (regenerated by reindex.sh)
    └── output.xml
```

## What is indexed vs. what is not

| Layer | Indexed? | Reason |
|-------|---------|--------|
| `knowledge/**/*.md` | ✅ Yes | Clean, distilled knowledge |
| `assets-index/**/*.md` | ✅ Yes | Searchable descriptions of binary assets |
| Top-level `*.md`, `kb.config.yml` | ✅ Yes | Agent context |
| `raw/**` | 🚫 No | Unprocessed, possibly sensitive |
| `processed/**` | 🚫 No | Intermediate stage, not curated |
| `assets/**` | 🚫 No | Binary, not LLM-readable |
| `review/**` | 🚫 No | Queue, not curated yet |
| `interactions/**` | 🚫 No | Session logs (privacy + noise) |
| `eval/**` | 🚫 No | Manual before/after probes, not knowledge |
| `sync/**` | 🚫 No | Transfer bundles and backups, not knowledge |
| `scripts/`, `shell/`, `templates/` | 🚫 No | Tooling, not knowledge |
| `log.md`, `lint-report.md` | 🚫 No | Operational state |

## Folder rules

### `knowledge/` — the heart of the base

Every file MUST have YAML frontmatter:

```yaml
---
source: "assets/documents/2026-05-06__q2-strategy.pdf"
extracted_at: 2026-05-06
last_verified: 2026-05-06
confidence: high                # high | medium | low
lifecycle: "evolving"           # permanent | evolving | temporal
importance: 7                   # 1-10
tags: [strategy, growth, q2-2026]
supersedes: null
---
```

See `11_PROVENANCE.md` for full frontmatter spec, `09_LINT.md` for validation rules.

### `raw/` — drop everything here

Subfolders are hints, not requirements. If unsure → `raw/unsorted/`.
The pipeline (`scripts/kb_ingest.py`) handles classification.
Files attached in chat must be confirmed by the user before being staged into `raw/<category>/unsorted/` for ingest.

### `review/` — agent's todo list

| Subfolder | When |
|-----------|------|
| `needs-classification/` | Pipeline couldn't determine type |
| `needs-ai-decision/` | Needs semantic analysis (complexity ≥ threshold) |
| `needs-redaction/` | Sensitive data detected |
| `needs-merge/` | An imported page and the local one both changed → `!merge` |
| `needs-heal/` | Upgrade left contents behind → `!heal` (`18_HEAL.md`) |
| `excluded-sensitive/` | Permanently excluded, archive only |

### `assets/` ↔ `assets-index/`

For every binary file in `assets/`, there's a Markdown description in `assets-index/<type>.md`.
The description goes into the index; the binary doesn't.

### `eval/` — before/after probes for instruction edits

Three typical questions in `QUESTIONS.md`. After a change to `AGENTS.md` or
the instruction modules, ask them in a fresh session and save answers in
`results/<date>__<what-changed>.md`. Not indexed. Do not gitignore `results/`.

### `sync/` — moving knowledge between machines

Only relevant when this base is deployed on more than one computer.

| Step | What you do |
|------|-------------|
| Export | Run `export.command` / `export.bat` / `./shell/export.sh` → bundle appears in `sync/outbox/` |
| Transfer | Copy that `.zip` to the other machine's `sync/inbox/` (USB, cloud folder, whatever you use) |
| Import | Run `import.command` / `import.bat` / `./shell/import.sh` there |
| Finish | Send `!merge` in the AI chat — the agent resolves conflicts and cross-links the new knowledge |

Nothing is ever overwritten silently: pages changed on both sides land in
`review/needs-merge/` with a diff, and every touched file is backed up to
`sync/backups/`. Details in `16_MERGE.md`.

## Stable filename convention

Files in `assets/` and `processed/` use:

```
YYYY-MM-DD__short-slug.ext
```

If the date is unknown:

```
unknown-date__short-slug.ext
```

The pipeline assigns these names automatically.

## Wikilinks

Files in `knowledge/` cross-reference each other:

| Format | Resolves to |
|--------|-------------|
| `[[slug]]` | Search `slug.md` across all `knowledge/` subfolders |
| `[[domain/caching]]` | Exact path: `knowledge/domain/caching.md` |

`kb_lint.py` validates all wikilinks.

## Routing tables

When `knowledge/` has > 50 files, create `knowledge/routing-table.md` and per-domain pages in `knowledge/routing/`.
See `05_INDEX.md`.
